# Използвайте официалния Node.js образ
FROM node:22

# Задайте работната директория в контейнера
WORKDIR /app

# Копирайте package.json и package-lock.json
COPY package*.json ./

# Инсталирайте зависимостите
RUN npm install

# Копирайте останалите файлове на приложението
COPY . .

# Уверете се, че портът, използван от приложението, е отворен
EXPOSE 5000

# Стартирайте приложението
CMD ["node", "index.js"]